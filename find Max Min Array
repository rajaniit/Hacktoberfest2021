int Solution::solve(vector<int> &A) {
    int N = A.size();
    int maxi = A[0];
    int mini = A[0];
    
    for(int i = 0; i<N; i++)
    {
        maxi = max(maxi, A[i]);
        mini = min(mini, A[i]);
    }
    return maxi+mini;
}
